import React from "react";
import { Link as ScrollLink } from "react-scroll";

const Header = () => {
  return <header className="w-full flex  items-center justify-center py-4 gap-8">
    <ScrollLink></ScrollLink>
  </header>;
};

export default Header;
